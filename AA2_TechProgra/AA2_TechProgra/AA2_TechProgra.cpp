#include <iostream>
#include <list>

int main()
{
	/*
	// Create the dynamic array 
	char** actualRoom;


	// Create a list of all our rooms 
	std::list<Room> rooms;
	for (int i = 0; i < static_cast<int>(TypeRoom::COUNT); ++i)
	{
		Room newRoom(static_cast<TypeRoom>(i), 5 * (i + 1), 5 * (i + 1));
		rooms.push_back(newRoom);
	}
	std::list<Room>::iterator itRooms = rooms.begin();

	// Init our player 
	Player player1(3, 3);


	actualRoom = itRooms->InicializeRoom(player1);

	itRooms->PrintRoom(actualRoom);
	*/
}
